# 제주너머 프로젝트 인수인계 문서

> 작성일: 2026-08-25  
> 프로젝트 경로: `C:\Users\horan\OneDrive\문서\Gemini 특강\ChatGPT\Beyond_Jeju\jeju-neomeo-agent`  
> 목적: 새 채팅에서 기획과 구현 맥락을 잃지 않고 바로 작업을 이어가기 위한 단일 기준 문서

기술 구조만 빠르게 확인하려면 같은 폴더의 `TECHNICAL_ARCHITECTURE_HANDOFF.md`를 함께 읽는다.

---

## 0. 새 채팅에서 가장 먼저 읽을 내용

현재 최우선 작업은 **구좌읍 두 번째 지점인 하도리 토끼섬의 상세 퀘스트 구현**이다.

하도리 확정 시나리오는 `docs/HADO_SCENARIO.md`에 저장되어 있다. 다음 작업자는 이 파일을 먼저 전부 읽고 그대로 구현 기준으로 사용한다. 멸치·수온 이야기와 차광막 설정을 다시 넣으면 안 된다.

직전 작업에서 다음 코드만 미완성으로 들어갔다.

- `web/game.js`의 전역 상태에 `state.hado` 추가
- `web/index.html`에 `#hado-screen` HTML 골격 추가
- `web/index.html`의 `game.js` 캐시 버전을 `v=25`로 변경
- 하도리 전용 JavaScript 진행 함수와 CSS는 아직 없음
- 현재 `loadQuest(1)`은 여전히 기존 범용 `quest-screen`을 연다. 즉, 새 `#hado-screen`은 아직 연결되지 않았다.

현재 임시 골격은 확정 시나리오와 다르므로 다음 채팅에서 수정해야 한다.

1. NPC 이름을 `문정해 삼춘`으로 변경
2. 기억의 그늘·차광막 미니게임을 제거
3. 바람 망원경으로 문주란꽃·모래와 현무암·난들여를 찾는 관찰 미니게임 구현
4. 기억 속 방문자에게 `꺾지 맙서`, `지켜줍서`를 활용해 보호 의사를 전하는 입력 구현

---

## 1. 프로젝트 한 줄 정의

**제주너머**는 설문대할망의 잃어버린 기억을 되찾기 위해 제주 각 권역과 마을의 고유한 생활문화·역사·제주어를 체험하고 기억의 조각을 모으는 데스크톱 2D 웹 어드벤처 게임이다.

위치 기반 게임이 아니다. 모든 이동, 탐험, 대화, 퀘스트, 미니게임은 웹게임 안에서 이루어진다.

---

## 2. 문제 정의와 제작 목적

### 문제

제주가 귤, 바다, 돌하르방, 예쁜 카페 같은 단편적인 관광 이미지로 소비되면서 다음 요소가 약해지고 있다.

- 마을마다 다른 고유 문화와 역사
- 제주어의 실제 사용과 전승
- 제주 내부의 지역별 정체성
- 주민 공동체가 이어온 생활문화

### 프로젝트 관점

제주의 지역 정체성은 ‘제주 전체의 하나의 이미지’가 아니라 **각 마을과 권역이 지닌 서로 다른 문화의 집합**이다.

따라서 유명 관광지를 나열하지 않고, 마을 고유 문화가 존재하는 실제 지점을 골라 상황극과 퀘스트로 전달한다.

### 해결 방식

- 이용자는 문화 설명을 수동적으로 읽기만 하지 않는다.
- NPC와 대화하고 미니게임을 수행한다.
- 배운 제주어 표현으로 직접 문장을 입력한다.
- Gemini 멀티 에이전트가 문장 의미와 제주어 표현을 순차적으로 판정한다.
- 한 권역을 완료하면 설문대할망의 기억 조각 1개를 얻는다.

---

## 3. 대상 이용자와 개발 제약

- 대상: 성인을 포함한 일반 이용자
- 플랫폼: 데스크톱 브라우저 전용
- 기준 화면: 1920×1200을 우선 고려하되 1920×900, 1440×900도 시각 테스트
- 모바일 구현은 우선순위가 아님
- 사운드: 개발 기간 때문에 현재는 넣지 않거나 최소화
- 초기 개발 기간: 약 2일짜리 발표 데모
- 발표 시간: 총 10분
- 사용자 입력 부담을 낮추기 위해 완전 자유 대화보다 **상황이 명확한 짧은 제주어 발화 연습**을 중심으로 설계

---

## 4. 핵심 세계관과 진행 규칙

### 플레이어

설문대할망의 잃어버린 기억을 찾아 나선 여행자.

### 전체 목표

- 제주의 14개 탐험 권역을 모두 완주
- 권역 하나당 기억의 조각 1개 획득
- 총 14개 기억의 조각을 모으면 설문대할망의 기억 완성

### 구좌읍 데모

구좌읍 안의 세 지점을 순서대로 탐험한다.

1. 송당리 본향당
2. 하도리 토끼섬
3. 동김녕리 해안

각 지점은 ‘기억의 흔적’ 1개를 준다. 세 흔적을 모두 모으면 구좌읍의 기억 조각인 **구좌의 바람 1/14**를 얻고 다음 권역인 조천읍이 해금된다.

중요한 규칙:

- 지점 하나를 깬다고 기억 퍼즐 전체가 완성되는 것이 아니다.
- 구좌읍 세 지점 완료 → 구좌읍 기억 조각 1개
- 전체 14개 권역 완료 → 기억 조각 14개 → 설문대할망의 기억 완성

---

## 5. 제주 전체 지도 구획

제주 전체를 43개 읍면동 버튼으로 동일하게 취급하지 않고, 발표와 게임 진행에 적합한 **14개 탐험 권역**으로 묶었다.

### 14개 권역 구성

- 읍 7개
  - 구좌읍
  - 조천읍
  - 애월읍
  - 한림읍
  - 대정읍
  - 남원읍
  - 성산읍
- 면 5개
  - 한경면
  - 안덕면
  - 표선면
  - 우도면
  - 추자면
- 동 권역 2개
  - 제주 열아홉 동네: 제주시 동지역 19개를 하나의 탐험 권역으로 묶음
  - 서귀포 열두 동네: 서귀포시 동지역 12개를 하나의 탐험 권역으로 묶음

화면 표기는 **“제주의 열네 권역”**으로 확정했다. ‘열네 동네’는 ‘제주 열아홉 동네’와 의미가 겹쳐 사용하지 않는다.

### 지도 원칙

- 실제 행정 경계를 기반으로 표현
- 구좌읍만 초기 해금
- 나머지 권역은 잠금 표시
- 마우스를 올릴 때만 해당 구역의 색 영역이 도드라짐
- 해금된 구좌읍과 이후 조천읍도 기본 상태에서는 색 영역을 고정 표시하지 않음
- 해금 상태는 주황색 이름 상자와 텍스트로 표시
- 추자면은 제주 본섬에서 떨어져 있으므로 별도 삽입 지도에 표시

### 지도 데이터

- 통계청 SGIS 행정동 경계 기반
- 가공 데이터: `vuski/admdongkor` 2026-07-01 GeoJSON
- 원자료: 공공누리 제1유형
- 가공 경계: CC BY 4.0
- 관련 파일: `web/assets/HangJeongDong_ver20260701.geojson`, `web/jeju-map-data.js`

---

## 6. 시각 디자인 방향

### 확정 스타일

**제주 민화·판화의 질감을 살린 따뜻한 2D 일러스트 어드벤처**

핵심 표현:

- 한지와 황토색 종이 질감
- 제주 바다의 짙은 청록색
- 먹선과 민화풍 파도·구름·오름·돌담
- 주황색을 핵심 강조색으로 사용
- 사진처럼 사실적이기보다 판화·민화처럼 선명한 회화 질감
- 너무 허접한 프로토타입처럼 보이지 않도록 캐릭터 크기와 전환 애니메이션 확보

### 제주 전체 지도 화면

- 게임 시작 시 별도 도입 바람새 대화 화면 없이 바로 제주 전체 지도 표시
- 제주도는 짙은 바다 위에 떠 있는 섬처럼 표현
- 섬 내부는 단순 색 채우기가 아니라 민화 지형·오름·밭·마을 이미지 질감 적용
- 상단: 제주너머, 열네 갈래 기억의 길, 기억의 조각 카운터
- 왼쪽: 제주의 열네 권역 안내 카드
- 왼쪽 아래: 바람새와 현재 탐험 카드
- 1~14 단계 가로 진행 바는 삭제
- 기억 개수는 텍스트 숫자뿐 아니라 조각 아이콘 슬롯으로 시각화

### 구좌읍 지도 화면

- 실제 구좌 지형의 상대적 방향을 반영한 민화 지도
- 지점 상대 위치:
  - 송당리 본향당: 내륙·중산간 쪽
  - 하도리 토끼섬: 동북쪽 해안
  - 동김녕리 해안: 서북쪽 해안
- 세 지점은 순차 해금
- 완료한 지점은 완료 상태 유지
- 구좌읍 전체 초기화 버튼은 처음부터 항상 표시
- 각 지점 화면 우측 상단에 ‘지점 전체 건너뛰기’ 버튼 제공

---

## 7. 등장인물

### 플레이어

- 설문대할망의 기억을 찾아 나선 여행자
- 대화창 화자 UI에서는 실사 얼굴 대신 사람 형태가 분명한 검은 실루엣
- 이름 표기: `플레이어`
- 부가 설명 문구는 대화창 화자 카드에서 삭제

### 바람새

- 제주의 바람에서 태어난 영특한 꼬마 정령
- 플레이어 곁을 날아다니는 진행·길잡이 NPC
- 지역 역사, 문화 맥락, 제주어 힌트를 설명
- 게임 진행 담당 NPC로, 각 미션의 현지 NPC와 역할을 분리
- 이미지: `web/assets/baramsae.png`

### 고인택 어르신

- 송당리 본향당 퀘스트의 현지 NPC
- 설정: 72세, 신목을 오래 지켜온 당지기
- 실제 인물이 아니라 송당 문화 전달을 위해 구성한 가상 복합 인물
- 이미지: `web/assets/npc-songdang-gointek.png`

### 문해심 삼춘

- 현재 코드 데이터에 존재하는 하도리 자연유산 지킴이 NPC
- 이미지: `web/assets/npc-hado.png`
- 이름과 상세 역할은 하도리 최종 시나리오 확정 시 다시 확인 필요

### 해녀 순덕

- 동김녕리 해안 퀘스트의 현지 NPC
- 이미지: `web/assets/npc-gimnyeong.png`
- 현재 상세 대화형 시나리오는 아직 미구현

---

## 8. 대화 UI와 입력 UX 규칙

### 대화 화면

- 화면 속 큰 캐릭터와 하단 대화창을 사용
- 말하는 화자에 따라 캐릭터 강조와 화면 포커스 전환
- 대화창 왼쪽에 화자 캐릭터 이미지와 이름 표시
- 화자 부가 설명은 대화창 카드에 표시하지 않음
- 대사가 길면 여러 노드로 나누고 ‘계속’으로 넘김
- 대화창이 배경 캐릭터를 과도하게 가리지 않도록 높이 제한

### 제주어·표준어 전환

- NPC 대사는 기본적으로 제주어만 표시
- 번역을 대사 아래에 동시에 노출하지 않음
- 대화 본문 시작 위치와 같은 왼쪽 정렬선 위에 `표준어` 버튼 배치
- 버튼을 누르면 같은 대화창의 내용이 표준어로 교체
- 다시 누르면 제주어로 돌아감

### 키보드 조작

- 계속: 버튼 클릭, Space, Enter, 오른쪽 방향키
- 뒤로: 버튼 클릭, 왼쪽 방향키
- 답변 제출: Enter
- 입력창 줄바꿈: Shift+Enter
- 판정 통과 후 자동으로 다음 장면으로 넘어가지 않음
- 제출 버튼이 `계속` 버튼으로 바뀌고 사용자가 직접 진행

### 스킵과 초기화

- 각 제주어 입력창에 `제주어 입력 건너뛰기` 버튼
- 각 지점 화면 구석에 `지점 전체 건너뛰기` 버튼
- 지점 전체 스킵 시 해당 지점을 완료 처리하고 다음 지점 해금
- 구좌 지도에는 처음부터 `초기화` 버튼 표시
- 초기화 시 세 지점 진행도, 시도 횟수, 송당·하도 상태를 초기화해야 함

---

## 9. 송당리 본향당 퀘스트: 현재 구현 상태

송당리는 세 지점 중 가장 상세하게 구현되어 있으며 발표에서 상세 시연할 대표 퀘스트다.

### 현재 흐름

1. 플레이어가 송당 본향당 숲 입구 도착
2. 바람새가 송당 본향당과 백주또 설명
3. `상세정보` 선택 시 송당의 중산간·오름·신들의 고향·당숲·여러 마을제 설명
4. 큰 팽나무인 폭낭 아래 고인택 어르신에게 접근
5. 제주어 첫 인사 입력
6. 설문대할망의 기억을 찾으러 온 목적 입력
7. 폭낭 아래 숲의 기억 흔적 발견
8. 송당 사람들이 마을의 안녕과 생업을 함께 빌고 문화를 전승해온 의미 설명
9. 하얀 지전 → 오색 물색 순서로 거는 미니게임
10. 지전물색을 걸고 기억이 깨어나기를 바라는 제주어 입력
11. `함께 지켜온 숲의 약속` 기억의 흔적 획득
12. 고인택 어르신께 감사하고 하도리로 떠난다는 제주어 입력
13. 송당리 완료, 하도리 해금

### 송당 제주어 입력 4개

1. 인사
   - 뜻: 당지기 어르신을 부르고 안부를 묻는 첫 인사
   - 필수 표현: `삼춘` + `안녕하우꽈?` 또는 `편안허우꽈?`
2. 방문 목적
   - 뜻: 설문대할망의 기억을 찾으러 왔고 단서를 아는지 질문
   - 표현: `할망`, `왔수다`, `삼춘`, `이서마씸?` 계열
3. 신목에 정성 전하기
   - 뜻: 지전물색을 걸었고 숲의 기억이 깨어나길 바람
   - 표현: `지전물색`, `걸었수다`, `줍서`
4. 감사와 출발
   - 뜻: 고인택 삼춘께 감사하고 하도리로 떠남
   - 표현: `고맙수다`, `삼춘`, `하도리`, `가보쿠다`

### 구현 파일

- 상세 노드: `web/game.js`의 `exactSongdangNodes`
- 연습 계약: `web/game.js`의 `songdangPracticeSteps`
- 렌더·진행 함수: `renderSongdangDialogue`, `advanceSongdang`, `showSongdangPractice`, `submitSongdangAnswer`
- 화면: `web/index.html`의 `#songdang-screen`
- 스타일: `web/styles.css`의 `.songdang-*`, `.dialogue-*`
- 서버 루브릭: `app/data/rubrics.json`의 `gujwa_songdang_01`

### 현재 판정 방식

송당 4개 입력은 모두 실제 `/api/evaluate-stream`으로 연결되어 있다.

1. 문장 전체 의미와 현재 대화 상황의 적절성 판정
2. 1단계 통과 시에만 제주어 필수 표현의 의미·용법 판정
3. 내부 신뢰성 검증 및 Python 최종 게이트
4. UI에는 문장 의미와 제주어 표현 두 단계만 간결하게 표시

무관한 단어를 제주어 키워드 뒤에 나열하면 1단계에서 통과하지 않아야 한다.

---

## 10. 하도리 토끼섬 퀘스트: 자료·방향·미완료 상태

### 지점 순서

하도리 토끼섬은 종달리 옛 소금밭을 대체한 두 번째 지점이다.

현재 순서:

`송당리 본향당 → 하도리 토끼섬 → 동김녕리 해안`

### 승인된 공식 근거

현재 `app/data/evidence.json`과 Agent Platform Search에 다음 근거가 있다.

- `hado_01`
  - 제주 토끼섬 문주란 자생지는 제주시 구좌읍 하도리 산85
  - 문주란 자생 식생 보호를 위해 1962년 천연기념물 지정
  - 출처 계열: 국가유산청 국가유산포털
- `hado_02`
  - 하도리 굴동포구 앞 작은 섬
  - 본래 이름 `난들여`
  - 여름에 하얀 문주란 꽃이 섬을 덮은 모습이 흰 토끼처럼 보여 토끼섬이라는 이름을 얻음
  - 출처 계열: 제주관광공사 비짓제주
- `hado_03`
  - 보호가 필요한 문주란 자생지
  - 직접 들어가는 체험보다 하도리 해안에서 관찰하며 자연유산 의미를 배우는 방식이 적절

### 현재 서버 루브릭

- 퀘스트 ID: `gujwa_hado_01`
- 질문 ID: `crinum_name_and_protection`
- 학습 목표:
  - 하얀 문주란 꽃과 토끼섬 이름의 관계
  - 문주란 자생지
  - 천연기념물 보호·보전 의미
- 금지 주장:
  - 토끼가 많이 살아서 토끼섬이라는 설명
  - 누구나 안전하게 걸어서 들어갈 수 있다는 설명
  - 관광을 위해 만든 인공 정원이라는 설명

### 원래 초안에 있었지만 근거가 부족한 내용

다음 내용은 사용자의 초기 시나리오 초안에는 있었으나 현재 승인 자료 `hado_01~03`이 뒷받침하지 않는다.

- 문주란 개화를 바닷물 수온 상승의 신호로 보았다는 주장
- 문주란이 피면 멸치 떼가 몰려온다고 선조들이 판단했다는 주장

이 내용을 최종 시나리오에 넣으려면 공식·지역 자료를 추가 조사하고 승인 근거를 보강해야 한다. 근거 없이 문화 사실로 단정하면 프로젝트의 할루시네이션 방어 평가와 충돌한다.

### 차광막 미니게임 주의

실제 천연기념물 보호구역에 플레이어가 들어가 물체를 치우는 인상을 주면 부적절하다. 차광막 요소를 유지하려면 다음 중 하나로 분명히 표현해야 한다.

- 실제 물체가 아니라 ‘기억을 가린 검은 흔적’을 해안에서 걷어내는 상징적 미니게임
- 또는 보호구역에 개입하지 않는 관찰·기록 맞추기 미니게임으로 교체

### 확정 시나리오

전체 대사와 판정 예시는 `docs/HADO_SCENARIO.md`를 단일 기준으로 사용한다.

핵심 흐름:

1. 하도리 굴동포구 도착과 토끼섬 소개
2. 선택형 문주란 상세정보
3. 가상 복합 인물 `문정해 삼춘`과 만남
4. 바람 망원경으로 세 관찰 기록 찾기
   - 하얀 문주란꽃
   - 모래와 검은 현무암
   - 오래된 이름 난들여
5. 기억 속 방문자가 문주란을 꺾으려는 잘못된 장면 발견
6. 방문자에게 꽃을 꺾지 말고 지켜달라는 뜻을 제주어로 전달
7. 문장 의미 판정 → 제주어 표현 판정
8. 기억 장면 복원
9. `기억의 흔적 ②: 바람과 모래가 지킨 하얀 꽃` 획득
10. 동김녕리 해안 해금

제주어 학습 표현 `꺾지 맙서`, `지켜줍서`는 시나리오용 초안이므로 서버 루브릭과 데이터 스토어에 최종 등록하기 전에 제주어 사전 또는 전문가 검수가 필요하다.

### 직전 구현 중단 상태

`web/index.html`에 다음을 포함한 `#hado-screen` 골격이 임시 추가되어 있다.

- 바람새와 과거 이름인 문해심 삼춘 캐릭터 자리
- 하도리 전용 하단 대화창
- 폐기할 기억의 그늘 걷어내기 미니게임 자리
- 제주어 입력창과 2단계 판정 UI
- 과거 이름인 하얀 꽃의 흔적 보상 화면

JavaScript와 CSS가 연결되지 않은 미완성 상태이며 확정 시나리오와도 다르다. `docs/HADO_SCENARIO.md`에 맞춰 수정해야 한다.

### 다음 채팅의 하도리 작업 절차

1. `docs/HADO_SCENARIO.md` 전체 읽기
2. 기존 임시 `#hado-screen`에서 차광막·문해심·과거 보상명을 제거
3. 확정 대사와 바람 망원경 관찰 미니게임 구현
4. 하도리 전용 노드·뒤로/계속·표준어 전환·키보드 조작 구현
5. 보호 의사 제주어 입력을 현재 Agent API 계약에 연결
6. `꺾지 맙서`, `지켜줍서` 근거 검수 후 루브릭 추가
7. 하도리 완료 시 `state.completed.add(1)` 및 동김녕리 해금
8. 1920×1200, 1920×900, 1440×900 시각 테스트
9. 에이전트 재배포는 사용자가 별도로 승인할 때 진행

---

## 11. 동김녕리 해안 퀘스트: 현재 상태

### 문화 소재

- 동김녕 잠수굿
  - 해녀의 안전과 해산물의 풍요를 기원하는 해녀 공동체 의례
- 멜후림소리
  - 멸치 그물을 함께 당길 때 한 사람이 메기고 여러 사람이 받는 어로 노동요

### 학습 목표

- 잠수굿과 노동요의 차이
- 안전 또는 풍요
- 협력과 공동체

### 현재 구현 상태

- 지도 지점과 배경·NPC 자산 존재
- 범용 `quest-screen`에서 자유 답변과 Gemini 판정 가능
- 송당처럼 대화형 상세 시나리오·미니게임은 아직 미구현
- 하도리 완료 후 세 번째 지점으로 해금되어야 함

---

## 12. 프런트엔드 기술 구조

### 기술

- 별도 프레임워크 없는 HTML/CSS/Vanilla JavaScript
- 단일 페이지 안에서 `.screen`을 전환
- FastAPI가 정적 파일 제공

### 핵심 파일

- `web/index.html`: 전체 화면 구조
- `web/styles.css`: 전체 지도, 구좌 지도, 송당, 범용 퀘스트, 보상 화면 스타일
- `web/game.js`: 게임 상태, 화면 전환, 대화 노드, 미니게임, 진행도, API 호출
- `web/agent-api.js`: 브라우저와 `/api/evaluate`, `/api/evaluate-stream` 사이 계약
- `web/jeju-map-data.js`: 제주 14개 권역 SVG 경계·라벨 데이터

### 전역 상태

`web/game.js`의 `state`가 현재 진행을 메모리에 저장한다.

- `currentQuest`
- `completed: Set`
- `attempts`
- `memoryPieces`
- `songdang`
- 직전 작업에서 미완성으로 추가된 `hado`

현재 브라우저 새로고침 시 상태를 영구 저장하지 않는다. 발표 데모에서는 초기화가 쉽다는 장점이 있지만, 필요하면 추후 `localStorage`를 추가할 수 있다.

### 해금 로직

- `state.completed.size`를 기준으로 다음 지점 하나만 활성화
- 완료 지점은 다시 들어가도 완료 상태 유지
- 세 지점 완료 시 보상 화면
- `finishGujwa()`에서 기억 조각 1/14 및 조천읍 해금

---

## 13. 브라우저–서버 API 계약

### 브라우저 요청

`web/agent-api.js`는 다음 값만 전달한다.

```json
{
  "quest_id": "gujwa_songdang_01",
  "question_id": "practice_greeting",
  "user_answer": "삼춘, 편안허우꽈?",
  "attempt": 1,
  "rubric_version": "1.0"
}
```

### 엔드포인트

- `GET /api/health`
- `POST /api/evaluate`
- `POST /api/evaluate-stream`

### 스트리밍

`/api/evaluate-stream`은 NDJSON으로 단계 이벤트와 최종 결과를 보낸다.

- `culture`: 문장 의미 단계
- `dialect`: 제주어 표현 단계
- 내부 `verify`: 신뢰성 단계
- UI는 사용자의 혼란을 줄이기 위해 문장 의미와 제주어 표현 두 단계만 표시

### UI와 에이전트 분리 원칙

화면 시나리오가 바뀌어도 `questId`, `questionId`, 입력 문장 계약을 유지하면 에이전트 호출 코드를 재사용할 수 있다. 새 질문을 추가할 때는 UI 코드에 판정 기준을 하드코딩하지 말고 `app/data/rubrics.json`에 질문 계약을 추가한다.

---

## 14. 현재 에이전트 구조

### 실제 코드 구조

현재 `app/agent.py`는 Google ADK `SequentialAgent` 안에서 세 LLM 에이전트를 순차 실행한다.

```text
사용자 문장
  ↓
1. culture_judge
   문장 전체 의미·현재 대화 상황·필요 시 문화 근거 판정
  ↓ 의미 통과 시에만
2. dialect_judge
   필수 제주어 표현의 사용 여부와 의미·용법 판정
  ↓
3. reliability_verifier
   근거 ID, 표현 ID, 상충, 조작 시도 교차 검증
  ↓
Python deterministic gate
   최종 verdict 확정
```

### 모델

- Vertex AI Gemini
- 코드 고정 모델: `gemini-3.6-flash`
- 위치: `global`
- 모델을 임의로 변경하지 말 것

### 1단계: 문장 의미 판정 에이전트

역할:

- 표준어·제주어 혼용 여부와 무관하게 문장 전체 의미 먼저 분석
- 현재 대화 상황에 맞는 말인지 검사
- 문화 설명 질문이면 승인 문화 근거와 필수 개념 대조
- 간단한 인사·감사·출발 상황이면 문화 RAG를 강제하지 않고 상황 의도 검사
- 제주어 키워드가 있어도 뒤에 무관한 단어를 나열하면 통과시키지 않음

### 2단계: 제주어 표현 판정 에이전트

1단계를 통과한 경우에만 실행한다.

역할:

- 제시된 제주어 필수 표현 그룹이 모두 사용됐는지 확인
- 단순 문자열 포함이 아니라 뜻과 사용 맥락이 맞는지 확인
- 잘못 사용한 제주어 표현 탐지
- 제주어 단어 하나만 섞고 표준어 격식 종결을 사용한 문장을 자동 통과시키지 않음
- `-습니다/-ㅂ니다/-입니다`로 끝나고 확인된 제주어 종결이 없으면 보완 판정 가능

### 3단계와 Python 게이트

실제 코드에는 신뢰성 검증 에이전트와 Python 결정론적 게이트가 유지된다.

중요한 발표 원칙:

- 발표 슬라이드와 게임 판독 UI에서는 핵심 경험인 **문장 의미 → 제주어 표현** 두 단계를 강조
- 신뢰성 검증과 Python 게이트를 삭제한 것이 아님
- 발표에서 너무 복잡하게 보이지 않도록 내부 안전장치로 설명하거나 생략

### 배포 단위

`jeju-neomeo-agent`라는 Agent Runtime 하나 안에 세 하위 ADK 에이전트가 들어 있다. Agent Runtime 리소스가 하나라고 단일 에이전트인 것은 아니다. 멀티 에이전트 여부는 배포 리소스 개수가 아니라 독립 역할·프롬프트·상태·실행 순서를 가진 하위 에이전트 구조로 판단한다.

---

## 15. RAG와 승인 근거 검색

### 검색 구조

현재 검색 백엔드는 두 가지를 지원한다.

- `local_json`
- `agent_platform_search`

실제 로컬 `.env`에는 Agent Platform Search와 Agent Runtime 관련 변수가 설정되어 있다. 값은 이 문서에 노출하지 않는다.

### 데이터 흐름

```text
질문·사용자 답변
  ↓
Agent Platform Search에서 후보 evidence_id 검색
  ↓
app/data/evidence.json 승인 레지스트리로 재검증
  ↓
현재 quest_id, 버전, 학습 목표에 허용된 근거만 에이전트에 전달
```

Agent Platform Search 결과를 그대로 진실로 신뢰하지 않는다. 원격 검색은 후보 ID 탐색용이며, 로컬 승인 레지스트리가 최종 허용 범위를 정한다.

### 핵심 데이터 파일

- `app/data/evidence.json`: 승인 문화 자료와 제주어 표현 근거
- `app/data/rubrics.json`: 퀘스트·질문·필수 개념·필수 제주어 그룹·금지 주장
- `app/data/hints.json`: 허용된 힌트 ID와 문장
- `artifacts/agent_search/evidence.jsonl`: 관리형 데이터 스토어 업로드용 문서

### Agent Platform Search 현황

- 승인 문서 13개 수집 이력
- 데이터 스토어: `jeju-neomeo-search-v2-collection_documents`
- collection: `jeju-neomeo-search-v2-collection`
- 기존 잘못 생성한 Search 앱은 삭제
- 별도 Search 앱을 호출하지 않고 데이터 스토어 serving config를 직접 검색
- 기존 Gemini Enterprise 앱에 데이터 스토어 연결

---

## 16. GCP와 배포 구조

### 현재 완료된 것

- GCP 프로젝트: `<GCP_PROJECT_ID>`
- Vertex AI Agent Runtime 배포 완료
- Runtime 리전: `us-east1`
- Runtime 리소스:
  - `projects/<PROJECT_NUMBER>/locations/us-east1/reasoningEngines/<AGENT_RUNTIME_ID>`
- Agent Platform Search 데이터 스토어 연결
- 기존 Gemini Enterprise 앱에 ADK 에이전트 등록
- 등록 표시 이름: `제주너머 문화·제주어 판정 에이전트`
- 배포 리소스/코드 명칭: `jeju-neomeo-agent`

### Gemini Enterprise 앱

- 기존 앱 ID: `<GEMINI_ENTERPRISE_APP_ID>`
- 별도로 만들었던 `제주너머 공식 근거 검색` Search 앱은 삭제됨
- 데이터 스토어는 기존 Gemini Enterprise 앱과 연결
- Agent Runtime의 ADK 에이전트도 기존 Gemini Enterprise 앱에 등록

### Cloud Run

사용자는 최종적으로 **Cloud Run 배포를 사용할 계획이라고 확정**했다.

목표 구조:

```text
사용자 브라우저
  ↓ HTTPS
Cloud Run: 게임 웹 + FastAPI 인증 프록시
  ↓ 인증된 서버 간 호출
Vertex AI Agent Runtime: ADK 멀티 에이전트
  ↔ Agent Platform Search 데이터 스토어
```

현재 주의점:

- Agent Runtime은 배포 완료
- 게임 서버의 Cloud Run 최종 배포는 아직 완료됐다고 가정하면 안 됨
- 현재 `Dockerfile`은 `app.fast_api_app:app`을 실행하며 Agent Runtime용 구성에 가깝고 `web/`을 복사하지 않는다.
- Cloud Run 게임 서버 배포 전에 게임 정적 파일을 포함하고 `app.local_game_server:app` 또는 별도의 Cloud Run용 진입점을 사용하도록 Dockerfile/배포 구성을 검토해야 한다.
- Cloud Run 최소 인스턴스는 비용을 줄이려면 데모 단계에서 0을 우선 고려한다. 과거 제안된 최소 1개는 유휴 비용이 발생한다.

### 인증 원칙

- 브라우저에 Google 인증정보와 Runtime ID를 노출하지 않음
- Cloud Run/FastAPI 서버가 인증된 요청으로 Runtime 호출
- 로컬에서는 Application Default Credentials 사용

---

## 17. 할루시네이션 방어와 신뢰성

발표에서는 사용자 문장 이해와 제주어 학습의 신뢰성에 집중한다.

### 핵심 방어 1: 의미 우선 게이트

- 제주어 키워드가 보인다고 바로 통과시키지 않음
- 문장 전체가 현재 상황에 맞는지 먼저 판정
- 무관 단어 나열, 의미 없는 문장, 잘못된 문화 설명을 분리
- 1단계가 실패하면 제주어 판정 생략

### 핵심 방어 2: 제주어 문맥 검증

- 단어 개수만 세지 않음
- 필수 표현이 뜻과 상황에 맞게 사용됐는지 확인
- 표준어 종결형과 잘못된 제주어 결합 탐지
- 의미는 맞지만 제주어가 부족한 경우에만 제주어 보완 힌트 제공

### 내부 안전장치

- 사용자 입력을 신뢰하지 않는 인용 데이터로 취급
- 명백한 프롬프트 공격은 모델 호출 전 차단
- Pydantic 구조화 출력
- 승인 근거 ID·특징 ID·힌트 ID 화이트리스트
- 검색 실패 시 조용히 로컬 데이터로 성공 처리하지 않고 503으로 중단
- 신뢰성 에이전트의 교차 검증
- Python 결정론적 최종 판정
- 낮은 신뢰도 또는 에이전트 상충 시 자동 통과 금지
- 네트워크 장애 시 가짜 성공 처리 금지

### 네 가지 발표 테스트 사례

1. 의미도 맞고 제주어도 맞음 → 두 단계 통과
2. 의미는 맞고 표준어 사용 → 의미 통과, 제주어 보완
3. 제주어 표현은 있으나 의미가 틀림 → 의미 보완, 제주어 검사 생략
4. 의미도 제주어도 틀림/무관함 → 상황에 맞는 의미부터 다시 안내

---

## 18. 평가 기준과 발표 전략

### 평가 기준 100점

- Multi-Agent 아키텍처 및 GCP 인프라 완성도: 35점
- 할루시네이션 방어 및 무결점 신뢰성: 20점
- 비즈니스 임팩트 및 문제 정의: 30점
- 팀 시너지 및 프레젠테이션: 15점

### 10분 발표에서 보여줄 것

- 문제: 마을 문화·제주어·지역 정체성 소실
- 해결: 14개 탐험 권역과 마을 문화 기반 어드벤처
- 라이브 데모: 구좌읍과 송당 상세 퀘스트
- 실제 Gemini 호출: 문장 의미 → 제주어 표현 순차 판정
- GCP 구조: Cloud Run 예정 게임 서버, Agent Runtime, Agent Platform Search
- 신뢰성: 의미 우선 검사, 제주어 문맥 검사, 승인 근거, 실패 시 진행 차단
- 확장성: 주민·기관 검수 루브릭을 추가해 14개 권역으로 확장

발표에서는 세 퀘스트를 모두 길게 보여주지 않는다. 송당 하나를 상세히 시연하고 나머지는 지점 해금·완료 흐름 중심으로 보여준다. 지점 전체 스킵 버튼은 발표 시간 조절용이다.

### 발표 자료 디자인 기준

원본 참고 파일:

```text
C:\Users\horan\OneDrive\문서\카카오톡 받은 파일\Beyond JEJU.pptx
```

발표용 기술 구조·할루시네이션 방어 이미지는 원본 PPT의 내용을 그대로 복제하는 것이 아니라 다음 시각 언어를 맞추는 방향으로 작업했다.

- 흰색 배경
- 굵은 검정색 산세리프 제목
- 주황색 소제목·상단선·화살표·단계 배지
- 얇은 회색 테두리와 부드러운 그림자의 흰 카드
- 좌측 상단 작은 주황색 섹션명
- 충분한 여백과 정렬된 카드 간격
- 장식용 모서리 격자무늬는 최종적으로 제거 요청됨
- 제목·본문 폰트 크기와 무게는 원본 기술 구현 구조 슬라이드와 통일

최종 발표에서 강조할 판정 도식은 다음 두 단계다.

```text
사용자 답변 → 01 문장 의미 판정 → 의미 통과 시 → 02 제주어 표현 판정 → 게임 피드백
```

실제 판정 예시 네 종류도 함께 보여준다.

- 의미 불일치
- 표준어만 사용
- 제주어 사용 맥락 오류
- 의미와 표현 모두 적합

---

## 19. 로컬 실행

### 요구 사항

- Python 3.11~3.13
- `uv`
- Google Cloud CLI
- Application Default Credentials
- 프로젝트 `.env`

### 실행 명령

```powershell
cd "C:\Users\horan\OneDrive\문서\Gemini 특강\ChatGPT\Beyond_Jeju\jeju-neomeo-agent"
gcloud auth application-default login
uv run uvicorn app.local_game_server:app --host 127.0.0.1 --port 8000
```

브라우저:

```text
http://127.0.0.1:8000/
```

또는:

```powershell
.\start-demo.ps1
```

상태 확인:

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
```

`execution_backend=agent_runtime`, `retrieval_backend=agent_platform_search`이면 로컬 게임 서버가 배포된 Runtime을 호출하는 구성이다.

---

## 20. 테스트

### 기본 테스트

```powershell
uv run pytest tests/unit tests/integration -q
node tests/ui_agent_api.mjs
```

### 시각 테스트

```powershell
node tests/ui_visual_smoke.mjs
```

기존 스크린샷은 `tests/`에 다음 해상도별로 존재한다.

- 1920×1200
- 1920×900
- 1440×900

### 배포 전 규칙

- Agent Runtime 배포는 반드시 사용자 명시 승인 후 실행
- 에이전트 코드 수정 후 단위·통합 테스트 선행
- 모델 이름 임의 변경 금지
- 같은 오류를 반복 배포로 해결하지 말고 원인 수정

---

## 21. 주요 파일 안내

```text
jeju-neomeo-agent/
├─ PROJECT_HANDOFF.md               # 이 인수인계 문서
├─ README.md                         # 일부 과거 내용이 있어 이 문서를 우선할 것
├─ pyproject.toml                    # Python/ADK 의존성
├─ agents-cli-manifest.yaml          # Agent Runtime 배포 설정
├─ deployment_metadata.json          # 현재 Runtime 리소스
├─ start-demo.ps1                    # 로컬 데모 실행
├─ app/
│  ├─ agent.py                       # ADK SequentialAgent와 세 하위 LLM 에이전트
│  ├─ evaluation_service.py          # 검색·에이전트 실행·최종 응답 조립
│  ├─ decision.py                    # Python 결정론적 최종 게이트
│  ├─ dialect_quality.py             # 제주어 품질 보강 규칙
│  ├─ guard.py                       # 입력 공격 사전 차단
│  ├─ evaluation_api.py              # /api/evaluate, /api/evaluate-stream
│  ├─ local_game_server.py           # 로컬 게임 정적 서버 + API 프록시
│  ├─ runtime_client.py              # Agent Runtime 호출
│  ├─ rubrics.py                     # 루브릭·근거 컨텍스트 구성
│  └─ data/
│     ├─ rubrics.json
│     ├─ evidence.json
│     └─ hints.json
├─ web/
│  ├─ index.html
│  ├─ styles.css
│  ├─ game.js
│  ├─ agent-api.js
│  ├─ jeju-map-data.js
│  └─ assets/
│     ├─ baramsae.png
│     ├─ scene-songdang-v2.png
│     ├─ scene-hado-v1.png
│     ├─ scene-gimnyeong-v2.png
│     ├─ npc-songdang-gointek.png
│     ├─ npc-hado.png
│     └─ npc-gimnyeong.png
├─ infra/agent_search/               # Agent Platform Search Terraform/스크립트
├─ scripts/                          # 데이터 export, 지도 생성, smoke test
├─ tests/                            # 단위·통합·UI·시각 테스트
└─ docs/
   ├─ AGENT_RUNTIME_DEPLOYMENT.md
   ├─ AGENT_SEARCH_SETUP.md
   ├─ DEMO_SCRIPT.md
   └─ EVAL_REPORT.md
```

---

## 22. 알려진 불일치와 기술 부채

### 문서 불일치

기존 `README.md`와 `docs/DEMO_SCRIPT.md`에는 과거 구조가 일부 남아 있다.

- 종달리 옛 소금밭 순서가 남아 있을 수 있음
- 과거 ParallelAgent 또는 3단계 UI 설명이 남아 있을 수 있음
- Cloud Run 미사용이라고 적힌 부분이 있으나 현재 사용자는 Cloud Run 최종 사용을 확정함

이 인수인계 문서를 최신 기준으로 우선한다. 구현이 안정되면 기존 문서도 정리한다.

### 에이전트 표현 불일치

- 코드 내부 설명에는 `상황·문화 의미 판정`이라는 과거 명칭이 남아 있음
- 사용자에게 보이는 UI는 `문장 의미 판정`으로 바꾸는 방향
- 내부적으로 문화 질문이면 문화 근거까지 검사하고, 상황 대화면 상황 의도만 검사하는 기능은 유지

### 하도리 화면 미완성

- HTML 골격만 존재
- CSS 없음
- 전용 JS 노드와 진행 함수 없음
- `loadQuest(1)` 연결 없음
- 시나리오 재확인 전 구현 금지

### 범용 퀘스트 화면

- 동김녕리는 아직 과거 범용 패널 UI
- 범용 패널에는 과거 3단계 파이프라인 UI가 남아 있음
- 향후 하도·동김녕을 송당식 대화형 화면으로 전환하면 정리 필요

### Git 상태

현재 상위 작업 폴더와 프로젝트 폴더에서 `.git` 저장소가 확인되지 않았다. 변경 이력을 Git으로 되돌릴 수 있다고 가정하지 말고 `apply_patch`로 범위를 좁혀 수정한다.

---

## 23. 다음 작업 우선순위

### 1순위: 하도리 시나리오 재확인

- 이전 추천 시나리오의 전체 대사·장면을 먼저 다시 보여주기
- 사용자가 “이 시나리오가 맞다”고 확인해야 구현 재개

### 2순위: 하도리 전용 플레이 구현

- 대화 노드
- 상세정보 분기
- 뒤로/계속
- 표준어 전환
- 캐릭터 전환
- 미니게임
- 제주어 입력과 판정 UI
- 통과 후 수동 계속
- 하얀 꽃의 흔적 보상
- 동김녕리 해금

### 3순위: 하도리 로컬 검증

- 에이전트 배포 없이 로컬 UI·시나리오 먼저 확인
- 현재 배포 Runtime과 질문 계약이 다르면 로컬 루브릭으로만 테스트하거나 입력 스킵 사용
- 최종 시나리오 확정 후 에이전트 데이터·테스트 수정

### 4순위: 동김녕리 상세 구현

- 하도리 UI 평가 후 같은 구조로 확장 여부 결정

### 5순위: Cloud Run 게임 서버 배포

- 정적 웹 포함 Dockerfile 또는 별도 서비스 구성
- 서비스 계정과 Runtime 호출 권한
- 최소 인스턴스 0 여부
- HTTPS 엔드포인트에서 종단 테스트

### 6순위: 발표 최종 점검

- 10분 동선
- 송당 Gemini 판정 네 사례
- 지점 스킵으로 세 지점 완료
- 기억 조각 1/14 및 조천읍 해금
- GCP Console에서 Runtime·Gemini Enterprise 등록 확인

---

## 24. 새 채팅 시작용 요청문

아래 문장을 새 채팅 첫 메시지에 붙여 넣으면 된다.

```text
프로젝트 루트의 PROJECT_HANDOFF.md와 docs/HADO_SCENARIO.md를 먼저 전부 읽고 현재 코드 상태를 확인해줘.
지금 최우선 작업은 구좌읍 두 번째 지점인 하도리 토끼섬 구현이야.
직전 채팅에서 하도리 HTML 골격만 일부 추가됐지만, 그 골격은 확정 시나리오와 달라.
docs/HADO_SCENARIO.md를 최종 기준으로 사용해서 문정해 삼춘, 바람 망원경 관찰 미니게임, 기억 속 방문자 보호 발화, 바람과 모래가 지킨 하얀 꽃 보상을 구현해줘.
멸치·수온·차광막 설정은 넣지 마.
에이전트 재배포와 Cloud Run 배포는 내가 별도로 승인하기 전에는 하지 마.
```

---

## 25. 최종 작업 원칙

- 문화 사실은 승인된 공식·지역 자료로 확인한다.
- 게임적 상상과 실제 문화 사실을 섞을 때는 허구임을 분명히 한다.
- UI 디자인 변경이 에이전트 재구현으로 이어지지 않도록 API 계약을 유지한다.
- 사용자 입력은 먼저 문장 전체 의미를 판정하고, 통과한 경우에만 제주어를 판정한다.
- 사용자가 결과를 읽기 전에 자동으로 다음 장면으로 넘기지 않는다.
- 발표용 예시 답안도 실제 Gemini 판정을 우회하지 않는다.
- 네트워크·검색·모델 오류를 성공으로 위장하지 않는다.
- 배포와 비용 발생 작업은 사용자 승인 후 실행한다.
